# Copy to .env.test. Used only by `pnpm db:push:test` to point the Prisma
# CLI at the test database. Must match TEST_DATABASE_URL in .env.
DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/amane_bio_test?schema=public"
